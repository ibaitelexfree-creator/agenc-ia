# Memory - 2026-06-29

- Discovered a translation issue on the courses page: when the language is set to English (or French), the courses displayed titles and descriptions in Basque because the fallback code in `CourseCard.tsx` and `CourseDetailPage` (`page.tsx`) checked `locale === 'es' ? course.nombre_es : course.nombre_eu` and fell back to Basque (`_eu`) for other locales.
- Added comprehensive course data translations (`courses_data`) for all 15 active courses to `en.json`, `fr.json`, `es.json`, and `eu.json` in both the root messages folder and the web app's messages folder.
- Replaced hardcoded UI strings on the course details page (such as "Reserva tu plaza", "Nivel", "Inversión", "Lo que aprenderás") with localized entries under the next-intl namespace `courses`.
- Refactored `CourseCard.tsx` and `CourseDetailPage` (`page.tsx`) to pull translated course name, description, and list details dynamically using the `courses_data` namespace, falling back gracefully to the database fields (first matching locale, then Spanish) if translations are missing.
- Discovered and resolved the same fallback translation issue in the rental system (`RentalCard.tsx` and `RentalClient.tsx`).
- Created and inserted `rentals_data` translation blocks for all active rental services into the JSON translation files for English, French, Spanish, and Basque.
- Cleaned up categories array in `RentalClient.tsx` by adding a localized `"atraques"` category to the translation files and fetching it via `t('categories.atraques')` instead of hardcoding `locale === 'eu' ? 'Atrakatzeak' : 'Atraques'`.
- Localized the product name inside the rental page's JSON-LD schema using the new `rentals_data` translations.
- Enabled direct course bookings without requiring date selection. Removed the date selector verification check from the booking action so users can reserve and pay for courses directly even if no calendar editions/dates are scheduled yet.
- Integrated flexible calendar date input for non-boat courses (e.g. windsurfing, rentals, paddle surf, etc.). Users can select any future date directly using a premium styled date input, which is then submitted as the reservation date. Boat courses (like J80, Raquero, Licencia) still use pre-scheduled calendar editions set by admins.
- Fixed the redirect flow for unauthenticated purchases by reading the `returnTo` search parameter in both the password and Google OAuth login routes. Passing `returnTo` dynamically to `redirectTo` inside [GoogleAuthButton.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/auth/GoogleAuthButton.tsx) and [RegisterForm.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/auth/RegisterForm.tsx) ensures customers land right back on the checkout/purchase funnel after signing in or confirming their emails.
- Adjusted style of the custom date picker input in [BookingSelector.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/booking/BookingSelector.tsx) from white text to dark grey (`text-sea-foam`) and a clean border (`border-black/10`) to provide high visibility contrast against the light page background.
- Refactored all white text classes and input border colors inside [LegalConsentModal.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/shared/LegalConsentModal.tsx) to use dark grey (`text-sea-foam`) and sub-opacities. This ensures that terms of agreement, document lists, input placeholders, DNI/DNI types, and checkbox descriptions are completely readable against the light bg of the modal container.
- Renamed PDF registration documents in `public/Documentos/Formularios inscripcion, LOPD y normas a firmar al contratar servicioos` to clean, web-safe filenames (removing spaces and invalid unicode accents like `` or `ó`). Updated paths in `LegalConsentModal.tsx` to be URL-encoded, ensuring that loading the PDF files inside the preview iframe works perfectly without resulting in connection refusal or 404 errors.
- Translated the general course terms contract into all supported languages (Spanish, English, Basque, and French), creating the `"course_contract"` translation keys in next-intl JSON files and calling them dynamically instead of loading a hardcoded Spanish string.
- Refactored all white text classes and input border colors in [RegistrationFormFields.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/booking/RegistrationFormFields.tsx) to dark grey (`text-sea-foam`), making the minor check question ("¿El alumno/a es menor de edad?") and all fields perfectly readable.
- Integrated database profile auto-fill logic into the Paso 2 (Formulario de Inscripción) step. Values like name, surnames, email, DNI, phone number, address, locality, postal code, and birthday are retrieved from the authenticated user's record and pre-filled in the registration form. These inputs remain fully editable and deletable so that users can make bookings on behalf of others.
- Fully localized the flexible date notice ("booking.flexible_date_notice"), form fields, placeholder names, select options, and error blocks inside [RegistrationFormFields.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/booking/RegistrationFormFields.tsx). Added respective translation blocks under `"registration_form"` namespace in the Next-intl locale JSON files for Spanish, Basque, English, and French.
- Localized the Step headers (e.g., "Paso 1 de 3") and the bottom navigation buttons ("Atrás", "Siguiente") inside the checkout legal modal [LegalConsentModal.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/shared/LegalConsentModal.tsx).
- Translated the form validation warning messages inside [LegalConsentModal.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/shared/LegalConsentModal.tsx) (e.g. "El código postal es obligatorio", "Seleccione si sabe nadar", etc.) by adding new keys under the `"validation"` namespace in Next-intl JSON translation files for all languages.
- Rephrased the hardcoded Spanish DNI/NIE validation check "La letra del DNI no es válida" in the checkout legal modal to be localized and general ("invalid_identity_document"), outputting "The ID is not valid, check it again" in English and correct equivalents in Spanish, Basque, and French.
- Extended identity document validation by implementing country-specific rules inside [validators.ts](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/lib/utils/validators.ts). Added a new country selection state and dropdown inside [LegalConsentModal.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/shared/LegalConsentModal.tsx), validating DNI/NIE/Passport numbers according to formatting rules of Spain, France, Germany, United Kingdom, Italy, Portugal, Andorra, United States, and general formats for other countries.
- Cleanly integrated the new country dropdown and fields in a balanced 2x2 grid layout inside Step 3 of the checkout modal.
- Fully localized the final remaining hardcoded Spanish strings inside [LegalConsentModal.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/shared/LegalConsentModal.tsx) (such as "Resumen de Condiciones y Normas Generales", "Firma Digital del Documento", and its description paragraph) using the next-intl hook across all languages.
- Added a Country of Origin select dropdown inside Step 2 Form [RegistrationFormFields.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/booking/RegistrationFormFields.tsx). Added verification logic to `handleNextStep` inside [LegalConsentModal.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/shared/LegalConsentModal.tsx) so that the user's DNI/NIE/Passport format and checksum numbers are correctly validated according to their selected country of origin when moving to the next step.
- Patched `validateIdentityDocument` in [validators.ts](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/lib/utils/validators.ts) to safely handle `undefined`, `null`, or blank string inputs without throwing a TypeError crash, and correctly aligned the `docType` variable for auto-detecting DNI vs. NIE vs. Passport types in Step 2.
- Changed the font size for error validation text messages inside [RegistrationFormFields.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/booking/RegistrationFormFields.tsx) from the unstyled/unsupported `text-3xs` class to a readable, standard `text-xs` class to make sure validation warnings are visible underneath the inputs.
- Implemented a payment method selector (Credit Card/Stripe vs. Bank Direct Debit) in Step 2 of the registration form inside [RegistrationFormFields.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/booking/RegistrationFormFields.tsx), hiding the bank account fields if Stripe is chosen, and making them dynamically required in [LegalConsentModal.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/shared/LegalConsentModal.tsx) only when the bank debit option is active.
- Fixed an overlapping layout bug in Step 3 of the checkout modal inside [LegalConsentModal.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/shared/LegalConsentModal.tsx) by adding narrow width constraints (`w-20 md:w-24`), minor padding reduction (`p-3`), and `flex-shrink-0` to the document type selector. Also added the `min-w-0` class on the adjacent DNI/Passport text input field to ensure the flex elements never exceed the grid boundary, aligning it perfectly with the right edge of the "Full Name" input box above it.
- Localized the Step 3 document type dropdown options (DNI, NIE, Passport/Pasaporte/Pasaportea/Passeport) in [LegalConsentModal.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/shared/LegalConsentModal.tsx) by creating new locale translation keys (`doc_dni`, `doc_nie`, `doc_passport`) and mapping them dynamically according to the active language.
- Wrapped the entire modal markup in [LegalConsentModal.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/shared/LegalConsentModal.tsx) inside a React Portal (`createPortal` rendering to `document.body`) to prevent parent stacking contexts (like `backdrop-blur` filters on the purchase card) from restricting the fixed overlay positioning, ensuring the modal always fills the screen and never clashes/overlaps with inline page content like footer newsletter forms.







