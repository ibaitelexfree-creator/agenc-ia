# Memory: 2026-07-23

## Summary
- Reverted the contact page text color change back to explicit black styling classes (`text-black`, `text-black/80`) instead of `text-foreground` for values.
- Completely removed the `hover:text-accent` and `group-hover:text-accent` transition behavior that was turning the values orange on hover.
- Kept/updated the label headers ("Location", "Email & Phone", "Hours") to be explicitly styled in the brand orange accent color (`text-accent`) as requested.
- Restored the hover effect for contact channels (`info@getxobelaeskola.com` and `(+34) 944 916 632`) so they instantly change to orange (`text-accent` with `duration-0`) when hovered.
- Executed a `git pull` on the `develop` branch to fetch the latest updates from the remote repository.
- Safely stashed local modifications (images, layout adjustments, and localization translation JSONs) prior to pulling, and successfully popped them back after the fast-forward merge with no conflicts.
- Updated [page.tsx](file:///c:/Users/farid/OneDrive/Desktop/agenc-ia/apps/getxobelaeskola-web/src/app/[locale]/contact/page.tsx):
   - Changed `text-foreground` on `h1` to `text-black`.
   - Set label headers (`location_label`, `contact_label`, `hours_label`) to `text-accent` (orange).
   - Changed value text colors (`location_val`, `contact values`, `hours_val`) to `text-black/80`.
   - Set instant brand orange hover color (`hover:text-accent transition-colors duration-0`) for interactive email and phone links.
- Fixed a browser crash (`STATUS_BREAKPOINT`) triggered when resizing the viewport or switching mobile emulations by removing a recursive `ResizeObserver` layout-loop in [Section1Hero.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section1Hero.tsx) and relying on a clean window `resize` event listener instead.
- Enforced sequential loading order in [Section1Hero.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section1Hero.tsx):
  1. Sky and Land/Sea render first.
  2. The Boat renders only after the background is loaded.
  3. The 5 Bottom Blobs render only after the Boat is fully loaded.
  4. Added a 3-second safety fallback timer to ensure content displays even on slow network conditions.
- Re-arranged the 5 interactive blobs into a 2-row Olympic-style grid (3 on top, 2 on the bottom) for screens under 768px (like the iPhone SE) to prevent overflow. The top row is shifted 18px to the right to leave clear spacing for the bottom-left accessibility widget.
- Shifted the main text container (from Eyebrow/Title to the main CTA button) upwards on mobile devices by adjusting `marginTop` and `top` offset, preventing it from overlapping with the top row of interactive blobs.
- Removed the blue fish (`Fish`) component from the Hero section for all screen sizes.
- Scaled down the white seagull (`Seagull`) by 20% (`scale(0.8)`) and shifted it 20% to the left (`right: '10%'` instead of `-10%`) on mobile screens to optimize screen space on the iPhone SE.
- Adjusted the white seagull (`Seagull`) layering by reducing `zIndex` from `8` to `2` to place it behind the boat (`zIndex: 3`) and in front of the background.
- Scaled down the entire boat container (`velero.webp` + membership access button) by 10% (`scale(0.9)`) on mobile screens (`isPhone` matches < 768px) to fit cleaner in the iPhone SE portrait viewport.
- Removed the gray windsurfer component (`Windsurfer`) with a blue sail from the top right corner of both [Section3Adapts.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Adapts.tsx) and [Section2Adapts.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section2Adapts.tsx) for all viewports.
- Pull-up and compacted the configuration controls layout in [Section3Adapts.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Adapts.tsx) by reducing top padding of the configurator block, decreasing the grid gap from 0.85rem to 0.5rem, and tightening internal paddings and gap sizes inside the option cards.
- Aligned the [ScrollToTop.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/shared/ScrollToTop.tsx) button with the WhatsApp button style in [LandingPageClientV2.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/home/LandingPageClientV2.tsx) by placing it in the exact same location (fixed bottom 24px, right 24px), scaling it to match (60x60px), and syncing the scroll visibility threshold to 400px so they morph seamlessly as the user scrolls.
- Configured the EqualWeb accessibility script in [AccessibilityScript.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/shared/AccessibilityScript.tsx) to position the widget exactly at the bottom-left corner with symmetric visual offsets on all pages using numeric strings (`vPosition: '99.8%'` and `margin: ['10', '10']` to avoid parser errors).
- Stopped the vertical floating loop animation of the logo in [Navbar.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/layout/Navbar.tsx) and fixed its position exactly 5px higher (`translateY(-5px)`) than the previous highest peak of the animation.
- Implemented layout adjustments for `Section3Adapts` in [Section3Adapts.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Adapts.tsx) and [SectionEyebrow.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/ui/SectionEyebrow.tsx) on mobile viewports (e.g. iPhone SE):
  1. Increased top padding from `clamp(2.5rem, ...)` to `6rem` to push the entire header down and avoid overlapping the header/navbar.
  2. Hidden the horizontal eyebrow line next to "La experiencia" using a conditional Tailwind class `hidden md:block`.
  3. Hidden the duplicate global "Leer más" / "Modern Sailing" link at the very bottom on screens under 768px (`!isPhone`).
- Configured conditional header hiding in [Section3Path.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Path.tsx): When "Intermediate Level" (mid) and "Adults" (adult) are selected together, the top header container (containing "Training" eyebrow, "Find Your Path" title, and "What's Your Level..." subtitle) is hidden, allowing the selector buttons and course cards to shift up to maximize space on smaller viewports.
- Adjusted Card 4 ("Vela Moderna") styling in [Section3Adapts.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Adapts.tsx) for mobile devices: Reduced width to `75%` and centered it (`margin: '0 auto'`) to prevent it from overlapping with the bottom-left accessibility widget and the bottom-right ScrollToTop/WhatsApp button.
- Re-structured `FlipCard` inside [Section4Why.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section4Why.tsx) on mobile screens:
  1. Reduced card height to `150px` (shorter card style) to ensure all three cards fit within the iPhone SE viewport.
  2. Moved the beige circle icon from the top of the card to the bottom-left corner next to the "Discover More" link.
  3. Changed typography scaling (smaller headings/paragraphs) and left-aligned the card front text content.
  4. Adjusted the card's back face padding and description font sizes to fit the shorter height format cleanly.
  5. Expanded cards width to `80vw` (occupying 80% of the viewport width) to make them look wider and prevent narrow squishing by writing a native CSS media query override inside [globals.css](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/app/globals.css).
- Repositioned the decorative crab (`Crab`) inside [Section4Why.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section4Why.tsx) on mobile viewports by shifting its position 15% further to the left (updating `right` from `10%` to `25%`) to keep it from overlapping underneath the bottom-right ScrollToTop/WhatsApp buttons.
- Applied a conditional relative shift (`translateY(7%)`) to the `.promise__inner` block in [Section4Why.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section4Why.tsx) on mobile viewports to push the section header and cards 7% further down, preventing the title from rendering underneath the top Navbar logo.
- Inverted the selection styling of "Youth" and "Adults" profile selector buttons in [Section3Path.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Path.tsx): By default, they render transparently (no white background, no contour) and only when selected do they display the white background with the gray border outline.
- Shifted the course cards list grid container in [Section3Path.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Path.tsx) on mobile screens by adding `paddingLeft: '20px'` to the grid container, shifting all cards to the right to keep their content completely clear of the bottom-left accessibility widget.
- Configured Level selector buttons ("Basic Level" and "Intermediate Level") in [Section3Path.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Path.tsx) to become half as tall (padding reduced from `1rem 1.5rem` to `0.5rem 1rem`), use a smaller font size (`0.8rem`), reduce the container's bottom margin to `0.6rem`, and remove their emojis (`⚓`, `🌊`) when "Intermediate Level Adults" is selected on mobile viewports. This prevents them from overlapping with the top logo.
- Applied a conditional relative shift (`translateY(6.5%)`) to the main content container in [Section3Path.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Path.tsx) when "Intermediate Level Adults" is active on mobile viewports. This shifts the entire block 6.5% further down to safely clear space underneath the top header/logo.
- Implemented a profile selector flashing feature in [Section3Path.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Path.tsx): If a user selects a Level (Basic or Intermediate) but doesn't choose a Profile (Youth or Adults) within 1.5 seconds, both profile buttons start flashing with a smooth white-blue pulsing glow (using Framer Motion scale, box-shadow, and background opacity loops). The flashing stops immediately once either button is clicked.
- Resolved layout jumping/flickering ("parpadeo") in [Section3Path.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Path.tsx) when toggling visibility of the header block. Instead of unmounting the header entirely via conditional rendering (`{!hideHeader && ...}`), wrapped it in a `motion.div` that smoothly animates height, opacity, and margin-bottom to 0 when hidden, sliding other components seamlessly without layout snaps.
- Re-ordered transitions and doubled show/hide durations in [Section3Path.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Path.tsx): When courses exit, they fade out and slide downwards (`y: 80` over `0.8s`) immediately. The header is configured to wait 1.0s (`delay: 1.0s`) before expanding (`duration: 1.2s`) to allow the courses to completely exit and the layout to settle down. When courses enter and header hides, the header shrinks immediately (`duration: 1.2s`) and courses wait for it (`delay: 1.0s`) before doing their stagger fade-in animation, leading to an extremely smooth, organic, and premium UI flow.
- Added a deferred state `isVisualCompact` in [Section3Path.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Path.tsx) to delay Level buttons scaling, container repositioning, and header elements showing up until the courses exit animation (0.8s duration) has fully completed. Added custom overshoot resorte transition curves (`cubic-bezier(0.34, 1.56, 0.64, 1)`) for both the main container's translation and the Level button scaling. Sequentially staggered the header components from bottom to top upon entrance: subtitle first (1.1s delay), title second (1.4s delay), and eyebrow last (1.7s delay) with 0.3s delay steps, preventing any overlaps or logo collisions.
- Replaced standard CSS transitions (`transition: all ...`) on the main content container and the Level buttons in [Section3Path.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Path.tsx) with Framer Motion's hardware-accelerated `layout` prop and custom spring configurations (`stiffness: 90/120, damping: 18/20`). This prevents browser reflow stutter and double-animation layout jumps, ensuring sub-pixel smooth, native iOS-grade fluid animations on mobile screens (iPhone SE).
- Applied the hardware-accelerated `layout` prop and spring transition configurations (`stiffness: 90, damping: 18`) to the header wrapper and all of its inner elements (eyebrow, title, subtitle) in [Section3Path.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Path.tsx). This allows Framer Motion to animate the height of the header wrapper dynamically as the staggered inner text elements fade/translate in, completely eliminating empty gaps and layout "teleportation" jumps during transitions.
- Adjusted parent `<section>` element layout to top-align content on mobile screens (`alignItems: 'flex-start'` and `paddingTop: '4.5rem'`) in [Section3Path.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Path.tsx). This anchors the vertical positioning of the container on iPhone SE viewports, preventing browser Flexbox centering from recalculating and aggressively jumping the buttons up/down during height changes, ensuring a stable, static base and premium sliding motion.
- Converted container translation value from a percentage (`6.5%`) to a fixed pixel translation offset (`-100px`, which shifts the container upwards by ~15% of the iPhone SE height) when compact. Since fixed pixel values do not depend on the dynamic height changes of the container, this completely prevents the content block from violently collapsing downwards and pulling back up during the exit transition of the courses.
- Reverted the parent `<section>` vertical alignment on mobile viewports back to standard vertical centering (`alignItems: 'center'`, removing top-alignment overrides) in [Section3Path.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Path.tsx). Removed `layout` tracking on the outer content container and replaced the translation with a positive fixed pixel offset (`y: 35` instead of `-100px`). This keeps all buttons perfectly centered at their original vertical position, allows the header and button layout animations to run natively, and avoids layout jumps when height collapses.
- Added the hardware-accelerated `layout` prop and custom spring transition configurations (`stiffness: 120, damping: 20`) to both the Profile selection container (`motion.div`) and the individual Profile buttons (`motion.button`) in [Section3Path.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section3Path.tsx). This integrates profile buttons into Framer Motion's unified layout projection context, guaranteeing they preserve gaps and slide smoothly relative to Level buttons and header resizing, completely eliminating overlaps and stutter jumps.
- Lifted the flipped state of [Section4Why.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section4Why.tsx) cards up to the parent component (`activeCardIndex`). Enforced mutual exclusivity: clicking card B automatically flips card A back. Configured cards to flip back when clicked again, and registered a global document click listener to automatically close any flipped card when clicking outside, ensuring they don't stay locked in place.
- Registered a `focusin` event listener inside [useScrollEngineV2.ts](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/hooks/useScrollEngineV2.ts) to capture element focus changes during keyboard Tab navigation. The listener automatically detects which `<section>` the focused element belongs to, compares it with the current active section index, and triggers a smooth scroll animation (`animateScroll`) to snap the viewport and update the active dots on the sidebar navigation indicator.
- Updated `handleFocusIn` in [useScrollEngineV2.ts](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/hooks/useScrollEngineV2.ts) to unconditionally call `animateScroll` on every focus event. This forces the scroll position to snap perfectly to the exact section centers (`SECTION_PROGRESS`), instantly correcting any arbitrary layout offsets caused by browser scroll-into-view behavior (such as showing 80% of one section and 20% of another).
- Refined focus section detection in [useScrollEngineV2.ts](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/hooks/useScrollEngineV2.ts) by adding the class `canvas-container` in [CanvasV2.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/layout/CanvasV2.tsx). Instead of querying generic `section` tags (which retrieves nested elements and shifts index calculations), `handleFocusIn` now queries direct children of `.canvas-container` (`target.closest('.canvas-container > *')`), ensuring a strict 1-to-1 mapping with `SECTION_PROGRESS` and solving section jumping or scroll boundary locking.
- Removed the red focus box skip-link element from [layout.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/app/[locale]/layout.tsx) since custom layout snapping intercepts scrolling.
- Replaced the `div` wrapper for the language dropdown trigger inside [Navbar.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/layout/Navbar.tsx) with a native `<button>` element equipped with custom arrow and escape key event handling, enabling clean keyboard Tab navigation and expansion.
- Removed the `(!isAtHero || isIntroActive)` condition from the "Members Access" button (`layoutId="acceso-button-wrapper"`) inside [Navbar.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/layout/Navbar.tsx), making it permanently visible and focusable in the desktop header as the last navigation action item.
- Implemented a click-outside listener (via document `click` listener inside a `useEffect` hook) and a focus blur handler (`onBlur` checking `!currentTarget.contains(relatedTarget)`) on the language dropdown container inside [Navbar.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/layout/Navbar.tsx). This guarantees the language dropdown automatically collapses if the user clicks anywhere else on the page or tabs out of the language buttons (e.g., navigating to the shopping cart).
- Configured focus-driven scroll transitions inside [useScrollEngineV2.ts](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/hooks/useScrollEngineV2.ts) to snap instantly (`animateScroll(targetScroll, 0)`). This matches the speed of native browser Tab focus jumps, immediately aligning the canvas coordinate translations and preventing scroll feedback loops that previously caused layout offsets and multi-section jumping.
- Upgraded the desktop navigation items in [Navbar.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/layout/Navbar.tsx) to prevent direct page redirection on category links that contain dropdown menus (e.g. Club, Servicios, Blog, Contacto). Instead, clicking or pressing Enter/Space toggles the dropdown panel. Implemented an `onBlur` blur focus listener (`!currentTarget.contains(relatedTarget)`) and a document `click` listener on the parent container class `.nav-item-container` to automatically collapse the active dropdown when focus leaves its sub-menu items (tabbing out) or when clicking outside.
- Removed the `opacity: 0` property from the hidden state variants of the `GlowButton` container in [Section2Identity.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section2Identity.tsx). This ensures the button remains at `opacity: 1` and is permanently focusable by the browser's accessibility tree, preventing Chrome/Firefox from skipping it during Tab transitions.
- Integrated focus blurring logic (`activeEl.blur()`) in the scroll animation handler `animateScroll` in [useScrollEngineV2.ts](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/hooks/useScrollEngineV2.ts) that triggers whenever the page transitions to a new section. If the currently focused element belongs to a section other than the target active one, it is automatically blurred. This stops the browser from fighting the scroll engine by scrolling the page back to keep off-screen focused elements in view, completely resolving scroll locking.
- Adjusted the responsive blob sizes and positions for iPad Mini (using both `isTabletPortrait` and `isTabletLandscape`) in [Section1Hero.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section1Hero.tsx), scaling them down by 5% and adjusting vertical offset downward by 2% to avoid overlapping the rightmost "Acceso Socias" button.
- Fixed a Framer Motion `layoutId` trajectory bug (a "waypoint" or "rebote") on the "Acceso Socias" button in [Section1Hero.tsx](file:///c:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section1Hero.tsx) by replacing the `x: '-50%', y: '-50%'` motion style properties with the native CSS `translate: '-50% -50%'` property. This perfectly aligns the button's layout bounding box and completely eliminates intermediate jumping during the initial transition from the Navbar.
>>>>>>> 9b971c0 (fix(home): update memory, project state, iPad layout and navbar layout transitions)
