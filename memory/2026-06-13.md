# 2026-06-13 Daily Log

## Changes Made
- **Reverted Scroll Engine to Stable State**: Overwrote [useScrollEngineV2.ts](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/hooks/useScrollEngineV2.ts) with the stable layout backup.
- **Improved Snap Scroll Fluidity**:
  - Replaced Framer Motion `animate(animY)` based transitions (which caused stuttering and scroll locking) with custom requestAnimationFrame scroll interpolation.
  - Switched the easing function from `easeInOutQuart` to `easeInOutSine` (Sine-based) which has a significantly lower peak speed in the center of the transition, eliminating the sudden fast "spring-like" acceleration phase.
  - Increased animation duration to `1800ms` for much slower, smoother, and highly premium slide changes.
  - Adjusted gesture lock window (`wheelTimeoutRef`) to `400ms` to fully absorb momentum/trackpad inertia, avoiding accidental section skips.
  - Softened the springs for prow rotation (`stiffness: 20`, `damping: 10`) and compass angles (`stiffness: 15`, `damping: 8`) to align with the slower page transition speed.
- **Adjusted CTA Section Position**:
  - Decreased the vertical shift offset of Section 5 from `+40px` to `+32px` in `useScrollEngineV2.ts` to shift the "Zarpa con nosotros" section exactly `8px` higher (`5px` in the first step and `3px` more in this step).
- **Added Footer Conventional Scroll Bypass**:
  - Modified wheel, touchmove, keydown and scroll listeners in [useScrollEngineV2.ts](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/hooks/useScrollEngineV2.ts) to detect when the user is at the bottom of the CTA section (max scroll range).
  - Bypasses snapping controls and allows conventional scrolling if the user scrolls down into the footer, or scrolls up within the footer region.
  - Synchronizes index tracking when returning to the snap zone.
- **Fixed Glitch & stuck scroll going up from Footer**:
  - Added code inside `handleWheel` to block default scroll behavior immediately when `isAnimatingRef.current` is true, preventing native inertial events from fighting the JS transition.
  - Added `forceTransition` check to trigger snap back up to Section 4 (index 3) when scrolling up from the footer even if it is the same continuous trackpad gesture, resolving the scroll lock issue.
- **Implemented Overflow locking during Snapping**:
  - Fixed issues where rapid mousewheel inputs generated un-blockable native scroll momentum that fought against `window.scrollTo` by temporarily setting `document.documentElement.style.overflow = 'hidden'` (and on `body`) when the snap animation starts, and restoring it to empty string on complete/unmount. This completely clears all native inertia when transitioning sections.
- **Verified Code Integrity**: Checked that the restored scroll hook compiles cleanly without any TypeScript issues.
