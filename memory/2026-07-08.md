# Memory: 2026-07-08

## Summary
- Replaced the simple MultiLayerParallaxCard implementation for Versión 6 with a custom `BoatParallaxCard` component.
- The `BoatParallaxCard` overlays the boat cutout image onto the dark sea background image using CSS blending effects (`mix-blend-screen` and brightness adjustments) to integrate them seamlessly.
- Configured a floating and rocking effect for the boat container using Framer Motion loop animations (`y: [0, -5, 0]` and `rotateZ: [0, 1.5, 0, -1.5, 0]`).
- Added three layers of animated foreground SVG waves with reduced opacity (`60%`) and slowed-down transition speeds to create a gentle/light sea wave effect.
- Verified that the codebase builds correctly without any new lint warnings or typescript errors.

## Details
- Target files:
  - `src/components/ui/BoatParallaxCard.tsx` (new component)
  - `src/app/[locale]/test-parallax/page.tsx` (updated page imports & layout)
- Background image: `/images/home/paralax-2/Black_image_to_sea_2K_202607081817.jpeg`
- Boat/foreground image: `/images/home/paralax-2/เรือและคนบนเรือ_2K_202607081818.jpeg`
