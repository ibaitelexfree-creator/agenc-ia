# Memory Log - 2026-07-08

## Tasks Completed (Local Development)
- Processed background removal on two parallax images in `apps/getxobelaeskola-web/public/images/home/paralax-2/` based on user green/red markings.
- Reverted all temporary background remover page and component files from `getxobelaeskola-web` to keep the code repository clean.
- Created standalone Vite + React + TS + Tailwind CSS (V4) project `web-tools` on the Desktop.
- Initialized local Git repository in `web-tools` and committed all files.
- Fixed canvas zoom alignment issue: Added `transformOrigin: 'top left'` to the canvas image wrapper so that scaling transformations align precisely with the cursor coordinate system.
- Fixed Undo/Redo history stack issue: Added `commitToHistory()` calls to preset selections, custom color blurs, and slider interactions.
- Added **Multi-Area Selective Workflows**:
  - Implemented dynamic WebGL2 uniforms in the fragment shader to handle up to 20 concurrent selection areas.
  - Added area boundary test loops directly inside the GPU fragment shader. Overlapping areas are resolved using top-most index dominance.
  - Implemented "Select Area" drawing tool: Releasing drag adds a new named editing zone.
  - Designed double-click/single-click canvas overlay borders to activate editing specific zones.
  - Added "Show/Hide Areas" outlines toggle on the main canvas toolbar.
  - Added collapsible right-hand "Areas" accordion sidebar list, letting the user select, navigate, edit, or delete any work area.
- Added **Global Window Drag & Drop Overlay**:
  - Registered full window dragenter, dragleave, and drop event listeners.
  - Implemented a fullscreen layout overlay that pops up with a blur background when dragging files.
- Added **Edge Choker Slider and Expanded Limits**:
  - Reordered sliders so that Color Suppression is listed above Edge Feather.
  - Expanded Edge Feather limit from 20px to 100px and Color Suppression limit from 100% to 200%.
  - Added a Matte Choker slider (0 to 5px range) with multi-tap HSL cross-neighbor alpha lookup loops inside the WebGL fragment shader to erode mask boundaries and eliminate black/dark outlines.
- Fixed WebGL Re-rendering Bug:
  - Added `choker` and all `global` configuration parameters (`globalTargetColor`, `globalLowerTolerance`, etc.) to the WebGL rendering `useEffect` dependency array, ensuring slider changes in both local and global modes immediately redraw the canvas.
- Added **Decimal Precision (50 Steps) on Choker**:
  - Configured Choker slider `step="0.1"`, allowing 50 precise values between `0.0` and `5.0`.
  - Formatted display label as `{choker.toFixed(1)}px`.
  - Upgraded the WebGL fragment shader's erosion filter to compute subpixel alpha weights based on float boundaries and circular search radiuses, enabling perfectly smooth border erosion.
- Added **Photoshop Vector Roto-Masks & Area Actions**:
  - Implemented custom coordinates solver using canvas client rect boundaries to compute exact coordinates under the mouse, eliminating the 40-50px offset offset bug.
  - Added Shape selectors to selection tool mode: **Rectangle**, **Circle**, and **Freehand (Polygon)**.
  - For Freehand Drawing: Click to add vertices. Clicking near the start point closes the path loop and creates the area.
  - Implemented drag handles for all active path vertices: users can select any area and drag its individual points to adjust vector masks in real-time.
  - Implemented an offscreen canvas rendering system to generate high-speed stenciling textures (`u_maskTex`) for GPU vector mask testing.
  - Added per-area action selector:
    - **Remove Background (Chroma):** Standard threshold extraction.
    - **Restore Original (Protect):** Forces original pixels to remain opaque, shielding them from other global removals.
    - **Clone Stamp (Tampón de Clonar):** Clones pixels from a separate, offset region. Added a draggable purple crosshair handle on screen to adjust clone coordinates in real-time.
- Fixed Gallery Loading Bug:
  - Replaced temporary Object URLs (`blob:http...` which expire after browser session resets) with base64 Data URLs using `FileReader` upon image importing.
  - Saved files are now fully persisted in IndexedDB and load successfully in subsequent browser sessions.
- Upgraded Gallery Interactions:
  - Made the entire gallery card container clickable to load the corresponding image.
  - Added `e.stopPropagation()` on the "Delete" button inside the card so that deleting a saved project does not trigger loading it.
- Made Instructions collapsible:
  - Created a collapsible guide box with an toggle header button and arrow indicator, collapsed by default (`isGuideExpanded = false`).
- Adjusted layout:
  - Removed the subtitle/eyebrow banner text "UTILIDAD CREATIVA" to push the main image canvas workspace and sidebars higher up on the screen.
- Fixed Left Sidebar Height Overflow:
  - Split the sidebar layout to make only the middle parameters section scrollable (`flex-1 overflow-y-auto`).
  - The header controls and the bottom Save/Export actions container are now sticky and always visible, preventing buttons from being cropped or pushed off screen.
- Re-architected Canvas Header & Floating Controls layout:
  - Moved the main Title ("Background Remover Pro") into the Canvas Toolbar, centered between "Ocultar Áreas" and "Mantener para Comparar", saving an entire row of page height and expanding the workspace grid to `h-[calc(100vh-2rem)]`.
  - Moved Undo/Redo into a high-visibility, clean white overlay box located in the top-left corner of the canvas workspace.
  - Moved Zoom controls into a premium semi-transparent dark glass overlay box in the top-right corner of the canvas workspace.
- Updated Color Suppression & Added AI Auto-Keyer:
  - Set default Color Suppression (Supresión de color) to `100%` initially for both global settings and new imports.
  - Added a high-tech, compact **AI** button to the color presets row (taking zero vertical space by transforming it into a 4-column grid).
  - Implemented the `runAutoAI()` auto-adjustment algorithm: samples and averages the four corners of the loaded image to detect the background color, automatically applying the computed hex value as the key `targetColor` along with optimal preset settings (Tolerances: 20/25, Feather: 3, Choker: 1.0, Spill: 100).
- Simplified Toolbar & Tool Modes:
  - Removed redundant "Gotero" (Eyedropper) and "Mover/Zoom" (Pan/Zoom) buttons from the Left Sidebar. The native custom color picker eyedropper and automated mouse wheel/drag interactions serve these functions seamlessly.
  - Moved the "Dibujar Área" (Draw Area) shape selectors directly into the top Canvas Toolbar next to the show/hide area outline toggle.
  - Drawn shapes (Rectangle, Circle, and Polygon) are represented by simple, compact icons (Square, Circle, Edit3) that highlight when active. Clicking an active shape button toggles it off, returning automatically to Pan/Zoom mode.
- Synchronized Preset Sliders & Parameter-Specific AI buttons:
  - Updated "Verde Croma" and "Negro" preset buttons to immediately update all corresponding slider variables (target color, tolerances, feathering, choker, and spill) in React state. This forces all physical sliders and potentiometer controls to update instantly on screen.
  - Implemented 5 individual AI adjustment buttons inside the title bar of each slider. Clicking one recalculates and applies optimized, AI-determined parameters for that specific control:
    - **Tolerancia Inferior:** Sets to 20%.
    - **Tolerancia Superior:** Sets to 25%.
    - **Supresión de Color:** Sets to 100%.
    - **Suavizado de Bordes:** Sets to 3px.
    - **Contracción de Máscara (Choker):** Sets to 1.0px.
- Fixed Choker Slider Styling:
  - Restored the missing CSS `className` styling parameters on the choker range input slider (`w-full accent-teal-500 bg-neutral-850 h-1.5 rounded-lg appearance-none cursor-pointer`) that was accidentally omitted during a merge conflict.
- Fixed Verde Croma Black-Out bug:
  - Changed the default `spill` (Supresión de color) parameter configuration from `100` to `0` inside the "Verde Croma" preset click handler. This leaves unremoved chroma green background pixels fully visible in green instead of forcing them black, so the user can easily see which areas remain to be adjusted.
- Fixed Drawing Offset & Handle Dragging:
  - Moved pointer handlers (`onPointerDown`, `onPointerMove`, `onPointerUp`) from the `<canvas>` element to the parent container wrapper `<div>` that contains both the canvas and the SVG interactive handles overlay.
  - This solves the 50px vertical offset bug during rectangle drawing (previously caused by sibling overlay elements intercepting coordinates) and makes the resizing handle nodes fully clickable and draggable in real-time.
  - Changed parameter event signatures inside handlers from `React.PointerEvent<HTMLCanvasElement>` to `React.PointerEvent<HTMLElement>` for complete TypeScript compliance.
- Fixed Layout Scaling & Bounding alignment:
  - Removed standard stretching behavior (`max-w-full max-h-full`) and `object-contain` letterboxing on the canvas element, making the parent wrapper `<div>` shrink-wrap to the exact rendered aspect ratio boundary of the canvas. This aligns the SVG overlay and drawn coordinate points with the canvas boundary perfectly, eliminating the remaining 20px letterboxing drawing offsets.
- Fixed Layered Background Removal Reset bug:
  - Redesigned the WebGL2 fragment shader stenciling logic so that pixels outside local vector areas (`areaIndex == -1`) fall back to the global keying configurations instead of remaining completely raw and unedited.
  - Local area adjustments now work as override layers on top of the global removal, preserving all edits across the rest of the image.
- Added Automatic Color Picker Popover Dismissal:
  - Attached a programmatic `.blur()` call to the native custom color input picker's `onChange` event. Once a color sample is clicked and confirmed (including when using the native browser eyedropper tool), the popup dismisses immediately instead of lingering.
- Verified build compiles successfully in 14.68s.

---

## Remote Merge - 2026-07-08

### Summary
- Replaced the simple MultiLayerParallaxCard implementation for Versión 6 with a custom `BoatParallaxCard` component.
- The `BoatParallaxCard` overlays the boat cutout image onto the dark sea background image using CSS blending effects (`mix-blend-screen` and brightness adjustments) to integrate them seamlessly.
- Configured a floating and rocking effect for the boat container using Framer Motion loop animations (`y: [0, -5, 0]` and `rotateZ: [0, 1.5, 0, -1.5, 0]`).
- Added three layers of animated foreground SVG waves with reduced opacity (`60%`) and slowed-down transition speeds to create a gentle/light sea wave effect.
- Verified that the codebase builds correctly without any new lint warnings or typescript errors.

### Details
- Target files:
  - `src/components/ui/BoatParallaxCard.tsx` (new component)
  - `src/app/[locale]/test-parallax/page.tsx` (updated page imports & layout)
- Background image: `/images/home/paralax-2/Black_image_to_sea_2K_202607081817.jpeg`
- Boat/foreground image: `/images/home/paralax-2/เรือและคนบนเรือ_2K_202607081818.jpeg`

## Updates
- Restored the original high-quality cutout base (`boat_cutout.png` to `boat_cutout_2K.png`) to ensure the people and boat are kept intact without any holes in hair or clothing.
- Cleanly removed the large black square next to the people on the right side (X: 1350-1850, Y: 380-1020).
- Cleaned the black spot to the right of the girl in the pink shirt with glasses at X: 470-634, Y: 497-637.
- Cleaned dark outlines and dust specks around the stainless steel railings and ropes using boundary shaving.
- Added a cache-busting query parameter (`?v=4`) to the `boatImage` path in `page.tsx` to force browser reload.
- Added wake and foam spray particle animations to BOTH the left and right sides of the boat hull.
- Enhanced the foreground waves to 4 layers with varying speed and opacity to make the sea look lively and realistic.
- Set the boat's looping floating/rocking animations to stationary, keeping only the mouse hover parallax effect active, while keeping the sea and splash animations dynamic.
