# Memory - 2026-06-19

- Modified `Navbar.tsx` to increase the size of the Instagram, Facebook, YouTube, and Shopping Cart icons in the top utility bar (`w-4.5 h-4.5` instead of `w-3.5 h-3.5`).
- Brightened social media brand colors with subtle drop shadows to make them pop.
- Made the cart icon default to white (`text-white hover:text-accent`) with a subtle glow for maximum visibility.
- Removed the black scroll background from the main navbar entirely, making it always transparent (`bg-transparent`) so that there is no black band behind the main menu links (Inicio, Club, Servicios, Blog, Contacto, Tienda) at any scroll position.
- Increased the size of the magnifying glass (Search), profile/login icon, and language selector dropdown chevron in the top utility bar to match the larger icons (`w-4.5 h-4.5` and `w-3.5 h-3.5`).
- Made the main language selector label ("ES") larger and bolder (`text-xs font-bold` instead of `text-[11px]`).
- Increased the dropdown option size inside the language list to `text-[11px] font-black` and widened the container `w-20` to ensure readability.
- Re-architected desktop layout to use a grid-based system (`xl:grid xl:grid-cols-[1fr_auto_1fr]`) to anchor the logo, center menu, and right actions separately. This solves the layout shift issue where the center menu items (Inicio, Club, etc.) would jump to the right when the "Acceso" button disappeared or appeared.
- Modified the access/login button in both `Section1Hero.tsx` and `Navbar.tsx` (linked via `layoutId="acceso-button-wrapper"`).
- In `Section1Hero.tsx` (Section 1 / sailboat button):
  - Displays a 2-line layout: ACCESO / SOCIAS.
  - Reduced letter spacing (`tracking-[0.12em]`) and gap (`leading-[0.85]`).
  - Added a red/white blinking color animation with glowing red text shadows.
  - Increased text size to `24px` (`text-[24px]`).
  - Shifted the position offset of `buttonLeft` 18px further to the left (changed subtraction from `-161` to `-179`).
- In `Navbar.tsx` (navbar login button when scrolled):
  - Displays a 2-line layout: ACCESO / SOCIAS.
  - Set the background to solid white (`bg-white hover:bg-neutral-200`) and the text to black (`text-black`).
  - Increased the text size to `12px` (`text-[12px]`) to make the letters larger.
  - Removed all animations (no blinking) so that the text is static and highly legible.
- Modified `BlobCard.tsx` (Hero Section 1 blobs):
  - Implemented deferred video loading that delays fetching video streams until after the page load event has completed.
  - Synchronized the reveal animation with the HTML5 `onLoadedData` event (and `readyState` check) so that the magical portal flash only triggers once the video's first frame is loaded and ready, eliminating any blank/black loading gaps.
  - Added a staggered entrance timeline per card based on its index, where the delay gap between consecutive reveals is a randomized duration between 400ms and 800ms (generated deterministically to prevent React hydration warnings).
  - Implemented a magical "fairy teleportation" entrance effect: a bright central light resplandor, an expanding shockwave, and a rotating fairy dust sparkle glow that reveals the blob at its peak with a spring reveal animation.
  - Added a robust 2.5-second fallback to force-reveal the card via fallback image in case of slow connection/network failures.
- Added "falling from the sky" bounce transitions to the Hero text layout:
  - Modified [AnimatedText.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/ui/AnimatedText.tsx) to support `"falling"` (characters) and `"falling-words"` (words) spring animations with slight rotation and blur filters.
  - Updated [SectionEyebrow.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/ui/SectionEyebrow.tsx) to make the location eyebrow letters ("Getxo · Bizkaia") fall sequentially.
  - Modified [Section1Hero.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section1Hero.tsx) to make the logo sail SVG bounce down, the logo text ("Getxo Bela Eskola") fall down, the main title sentences ("No somos una escuela. Somos tu mar.") drop down character-by-character, and the subtitle paragraphs drop down word-by-word with staggered delays.
  - Staggered the main CTA button entrance to fade-up smoothly at `2.6s` after the texts finish landing.
- Processed the user's custom cloud JPEG graphics with black backgrounds:
  - Created a Node.js utility using `sharp` to downscale `Nuve_with_sun_202606191627.jpeg` and `Nube_de_verano_realista_fondo_202606191627.jpeg` to a web-optimized 250px width.
  - Extracted transparency from both JPEGs, filtering out their black backgrounds (`threshold: r,g,b < 35`) to yield clean transparent PNGs (`cloud_sun_mini.png` and `cloud_only_mini.png`).
- Applied logo cloud hover and position animation in `Navbar.tsx`:
  - Positioned `cloud_only_mini.png` under the navbar logo by default.
  - Added `cloud_sun_mini.png` behind it, animating its opacity and shifting it slightly up-right (`y: -10`, `x: 8`) when the logo is hovered, revealing the sun peeking out from behind the cloud.
  - Wrapped the entire logo container in a `motion.div` that shifts down and right. Updated the `y` translation from `isAtHero ? 15 : 0` to `isAtHero ? 23 : 8` to lower the entire logo and cloud assets by 8px in all states.
  - Scaled the cloud size to `w-[195%]` (15% smaller than the previous 230% size) and shifted its layout coordinates down 15px (to `bottom-[-55px]`) and to the left 16px (to `left-[-80px]`).
  - Modified hover logic to perform a clean in-place crossfade (opacity transition) between the two clouds (`cloud_only_mini.png` fading out, `cloud_sun_mini.png` fading in) without any positional shift.
  - Generated and processed all horizontal logo versions:
    - Updated `Logo Bela horizontal SIN FONDO.png` to map its original red/maroon sail pixels to the official website color `#BE0040` (RGB: 190, 0, 64), leaving the black text and transparency untouched.
    - Updated `Logo_Bela_horizontal_white_text.png` to also map the red/maroon sail pixels to `#BE0040` while keeping the white text intact.
  - Modified the image source in `Navbar.tsx` to dynamically load `Logo_Bela_horizontal_white_text.png` on hover during Section 1 (`isAtHero && isHovered`), keeping the original PNG asset for standard presentation.
  - Refined the image processing algorithm in `process_logo_precise.js` to strictly preserve the exact color codes of the original maroon red sail channels (using ratio checks `R > G * 1.15` and `R > B * 1.15`), preventing any white bleeding or alterations on the red logo elements.
  - Updated the image processing script to map all reddish/maroon pixels in the hovered logo (`Logo_Bela_horizontal_white_text.png`) to the official website color `#BE0040` (RGB: 190, 0, 64), matching the official design specification.
- Updated `Section2Identity.tsx` and translation files (ES, EN, FR, EU) to format the second value sentence:
  - Match style, font size (`clamp(1.1rem, 2.5vw, 1.45rem)`), weight (`300`), and color (`rgba(255,255,255,0.9)`) with the first values paragraph.
  - Remove italic font styling (`fontStyle: 'italic'` -> none).
  - Supported next-intl rich text formatting via `t.rich('value2')`.
  - Added HTML `<gold>` tags inside `value2` string translations to highlight "aprender a navegar" and "disfrutar" in the website's gold accent color (`var(--gbe-gold)`) with semibold weight and a glowing gold text-shadow.
- Removed the eyebrow element (`t('eyebrow')` / "Quiénes somos") from `Section2Identity.tsx` entirely.
