# 2026-06-19 Session Notes

## Objective
1. Update the "Getxo Bela Eskola" logo and text color in Section 1 (Hero) on the home page to white.
2. Align all written content in Section 1 (Hero) to the left (sea area).
3. Increase all font sizes in Section 1 (Hero) elements by 20% to improve visual impact and legibility.
4. Scale up the 5 interactive blobs by 25% and group them to the left of the text content without shifting the text's original position.
5. Overlap the blobs using negative margins/gaps to fit vertically inside the viewport and look like clustered droplets.
6. Position the 5 blobs in a "C" shape (Row 1 has Club and Cursos, Row 2 has Equipos, and Row 3 has Campamentos and Colabora) and offset them vertically (`top: 55%`, `marginTop: 50px`) to keep them completely clear of the navbar.
7. Lower the text container down by 100px (changing `marginTop` from `-120px` to `-20px`), shift it left by 50px (`left: '-50px'`), and render the title in exactly two lines.
8. Format the subtitle into exactly 3 lines of text on desktop screens.
9. Increase the font size of the texts inside the BlobCards further to improve visibility.
10. Bring the blobs closer together vertically by setting the negative vertical gap to `-5.25rem`.
11. Position the blobs container relative to the left of the text by anchoring it at `left: '-410px'`. This fixes the horizontal boundary of the first column in place.
12. Set the horizontal gap of both double-card rows (Row 1 and Row 3) to exactly `30px` to separate the columns.
13. Reduce the gap between the SVG bubble and labels inside the BlobCard, and the margin between its title and subtitle.
14. Animate the SVG bubble shape dynamically to sway/float gently using Framer Motion (with desynchronized, title-hashed durations), leaving the label texts completely static.
15. Synchronize the floating animation loop of the blobs with the boat's 12-second swaying cycle using unique phases and amplitudes for each card.
16. Implement magnetic attraction on the BlobCards to follow the cursor gently, and scale up only the SVG bubble/video elements on hover.
17. Increase the independent float/sway amplitudes of the SVG bubbles inside the cards to make their physical movement with the tides much more pronounced and visible.
18. Co-align the vertical floating phase of all blobs so they rise and fall in unison with the ocean tide/sailboat, while varying their horizontal angles (left, right, straight) individually.
19. Make the magnetic cursor tracking for the BlobCards slower and lazier by customizing the hook's spring configuration (`stiffness: 60`, `damping: 15`, `mass: 1.2`) to simulate a fluid water effect.
20. Remove the "Descubrir →" action link at the bottom of the card inside BlobCard.tsx.
21. Reduce the upward vertical float sway by 5px to prevent blobs from colliding with text labels of the cards above them.

## Actions Taken
1. Modified `LogoGBE` inside [Section1Hero.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section1Hero.tsx):
   - Changed path `fill` attributes from `black` to `white`.
   - Changed line `stroke` attribute from `black` to `white`.
   - Changed text `color` style property from `black` to `white`.
   - Increased `LogoGBE` text `fontSize` by 20% (from `1.1rem` to `1.32rem`).
2. Modified main content structure inside [Section1Hero.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section1Hero.tsx):
   - Restored the main container's original vertical/horizontal flow layout and `maxWidth: '650px'` so the text stays exactly in the same place.
   - Absolutely positioned the blobs wrapper to the left of the text container using `position: 'absolute'`, `left: '-410px'`, `top: '55%'`, `transform: 'translateY(-50%)'`, and shifted it down by 50px using `marginTop: '50px'`.
   - Shifted the text container down by 100px (setting `marginTop: '-20px'`) and to the left by 50px (setting `left: '-50px'`).
   - Split the title `h1` string dynamically on `". "` and rendered each sentence in a block `<span>` with `whiteSpace: isMobile ? 'normal' : 'nowrap'` on desktop to guarantee exactly 2 lines.
   - Formatted the subtitle dynamically on desktop to render exactly 3 lines matching the user's specific requested split, while retaining translation behavior for mobile.
   - Arranged the 5 cards in a "C" shape layout: Row 1 has Club (Card 1) and Cursos (Card 0) side-by-side with a horizontal gap of `30px`. Row 2 contains Equipos (Card 2) aligned on the left axis. Row 3 contains Campamentos (Card 3) and Colabora (Card 4) side-by-side with a horizontal gap of `30px`.
   - Updated the vertical container to use `alignItems: 'flex-start'` and a `-5.25rem` flex gap to overlap the rows vertically closer together.
   - Updated Eyebrow styling with `justifyContent: 'flex-start'`.
   - Changed Title and Subtitle styling to `textAlign: 'left'`.
   - Changed Subtitle margin to `0 0 2rem` to prevent auto-centering behavior.
   - Increased Title `fontSize` by 20% (from `clamp(2.2rem, 5vw, 4.5rem)` to `clamp(2.64rem, 6vw, 5.4rem)`).
   - Increased Subtitle `fontSize` by 20% (from `clamp(1rem, 2vw, 1.2rem)` to `clamp(1.2rem, 2.4vw, 1.44rem)`) and slightly expanded `maxWidth` to `670px` for optimal wrapping.
   - Upgraded main CTA button `GlowButton` size from `md` to `xl` (which handles the 20% increase to `1.2rem` font size).
3. Upgraded sub-components:
   - Added support for a custom `fontSize` prop in [SectionEyebrow.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/ui/SectionEyebrow.tsx) (used to increase the "Getxo · Bizkaia" eyebrow size to `0.85rem` inside the Hero).
   - Modified [GlowButton.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/ui/GlowButton.tsx) to support an `xl` size (matching `1.2rem` font size).
   - Increased typography size scales in [BlobCard.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/blobs/BlobCard.tsx): title to `text-sm sm:text-base md:text-lg`, subtitles to `text-sm sm:text-base`.
   - Modified [BlobCard.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/blobs/BlobCard.tsx) to change container gap to `gap-2` (reducing the vertical space between the bubble SVG and labels), and subtitle top margin to `mt-[2px]` (reducing space between the title and description).
   - Wrapped the SVG element inside [BlobCard.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/blobs/BlobCard.tsx) in a `<motion.div>` that performs an organic vertical, horizontal, and rotational floating sway (`x`, `y`, `rotate`) using a 12-second duration synchronized with the boat, with unique offsets/phases computed dynamically based on each title.
   - Integrated the `useMagneticCursor` hook inside [BlobCard.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/blobs/BlobCard.tsx) to allow cards to smoothly follow the mouse on hover.
   - Updated the SVG `<motion.div>` container inside [BlobCard.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/blobs/BlobCard.tsx) to animate its `scale` dynamically up to `1.18` on hover with a spring transition, leaving label texts at scale 1.0.
   - Increased the amplitudes in [BlobCard.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/blobs/BlobCard.tsx) for the independent sway variables: horizontal amplitude to `7px-15px`, vertical amplitude to `12px-27px`, and rotational amplitude to `2.5°-5.5°` to make the floating motion more pronounced.
   - Replaced phaseDir in vertical/horizontal keyframes inside [BlobCard.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/blobs/BlobCard.tsx) to synchronize vertical direction with the boat/tide (all go up together, then down together), while varying their diagonal angles (x-axis factor) individually.
   - Updated [useMagneticCursor.ts](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/hooks/useMagneticCursor.ts) to accept optional spring physics overrides (`stiffness`, `damping`, `mass`) to support dynamic physics.
   - Overrode the default spring physics in [BlobCard.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/blobs/BlobCard.tsx) for magnetic tracking with slower, high-inertia values (`stiffness: 60`, `damping: 15`, `mass: 1.2`).
   - Removed the "Descubrir →" span from the footer of [BlobCard.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/blobs/BlobCard.tsx).
   - Reduced the vertical float's upward Y keyframe inside [BlobCard.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/blobs/BlobCard.tsx) to `-(yAmplitude - 5)` to prevent collisions with the card labels above.
4. Resized Blobs:
   - Increased the width/height sizes of the SVG blobs inside [BlobCard.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/blobs/BlobCard.tsx) to `w-20 h-20 sm:w-24 sm:h-24 md:w-28 md:h-28 lg:w-40 lg:h-40` (a ~25% increase).
5. Decoupled Wave Physics & Pronounced Lateral Randomness:
   - Implemented an asynchronous client-side random vector generator in a `useEffect` hook inside [BlobCard.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/blobs/BlobCard.tsx) configured on a double-cycle 24-second timeline.
   - Generated 4 independent vertical amplitudes (`yAmpRise1`, `yAmpFall1`, `yAmpRise2`, `yAmpFall2` between 10px and 25px) and 4 completely independent horizontal drift offsets (`xOffsetRise1`, `xOffsetFall1`, `xOffsetRise2`, `xOffsetFall2` between -35px and +35px) per card.
   - This ensures that a single card travels on a completely different diagonal path for every successive stroke (e.g. drifting left on the first rise, then right on the first fall, and on different diagonal routes for the second cycle), making it highly organic and unpredictable while keeping all cards synchronized in overall phase with the boat and tide.
   - Solved Next.js hydration issues by using client-side initialization inside the `useEffect` hook.
 6. Hero Text Scale-Up & Left Alignment Adjustment:
   - Moved the main text block container in [Section1Hero.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section1Hero.tsx) an additional 20px to the left (updating `left` from `-50px` to `-70px`).
   - Scaled up the eyebrow text size to `0.95rem` and the school name logo text to `1.45rem` (increasing the SVG icon size to `40x40px`).
   - Increased the title font size to `clamp(2.9rem, 6.5vw, 6.0rem)` and the subtitle font size to `clamp(1.3rem, 2.6vw, 1.6rem)`.
   - Expanded the subtitle container `maxWidth` to `710px` to ensure the 3-line layout wraps perfectly.
   - Added a new `"xxl"` size variant inside [GlowButton.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/ui/GlowButton.tsx) (`1.35rem` font size and `1.3rem 4rem` padding) and updated the main CTA button to use it.

