# Daily Log - 2026-07-03

- Configured the local Git workspace to track and target the `develop` branch instead of `main` by checking out `develop` (tracking `origin/develop`).
- Created a Git `pre-push` hook at `.git/hooks/pre-push` to prevent accidental direct pushes to the `main` branch in the future.
- Performed a test commit and push to `develop` successfully by adding a verify comment to `src/components/decorative/HorizonLine.tsx`.
- Reverted duplicate change in root `src/components/sections/Section4Why.tsx`.
- Redesigned the pillars in `apps/getxobelaeskola-web/src/components/sections/Section4Why.tsx` to display as centered, square-shaped cards side-by-side on desktop (using responsive CSS Grid and `md:aspect-square`), while maintaining vertical stacks on mobile viewports for optimal readability, updating both the Front and Back sides of the flip cards.
- Refactored the "Why sail with us?" section according to updated specifications:
  - Display cards in a single row on desktop, 2 per row on tablet (`sm:grid-cols-2`), and 1 per row on mobile.
  - Enforced perfect 1:1 aspect ratio (`aspect-square`) and equal width and height across all cards.
  - Placed icon at the top, followed by heading, description, and added a "Learn More" (Saber más / Gehiago ikasi / En savoir plus) link at the bottom.
  - Replaced the background color with a clean white, added 20px rounded corners, and a subtle soft shadow.
  - Implemented a smooth hover lift animation with increased shadow.
  - Localized the "Learn More" link in `en.json`, `es.json`, `eu.json`, and `fr.json`.
- Implemented premium interactive and visual redesign of Section 4 cards:
  - Increased card sizes by 20-30% on desktop (increased max-width to `1350px`, gap to `2.25rem`, and padding to `2.5rem 2rem`).
  - Implemented Apple/Stripe-inspired glassmorphism styles (`backgroundColor: 'rgba(255, 255, 255, 0.78)'`, `backdropFilter: 'blur(20px)'`, 26px rounded corners, and custom border/shadow configurations).
  - Implemented rich hover effects: smoothly lifts by 14px, scales up to `1.03`, glows border color (`rgba(10, 126, 200, 0.35)`), and increases shadow depth.
  - Configured child hover variant propagation: icon gently bounces and wiggles, and "Learn More →" button reveals with a smooth fade-up animation.
  - Preserved all localization texts and fonts.
- Implemented vibrant ocean-inspired styling and click interactions for the cards:
  - Set custom linear gradient backgrounds on each card (Sky/Light Blue, Mint/Turquoise, and Yellow/Sand).
  - Added subtle wave SVGs inside each card to enhance the nautical atmosphere.
  - Configured click interactions managed by parent index state (expanding the active card, collapsing others, triggering quick icon pulse animation, and highlighting with a colorful glowing border/shadow).
  - Replaced the text link with a modern rounded button with animated arrows pointing forward on hover, matching brand accent colors.
- Fixed a compilation error on Vercel build caused by duplicate imports of `useState` and duplicate definition of `pillars` at the top of `Section4Why.tsx`.
- Redesigned Section 4 with a 3D Captain's Logbook / Sailing Journal opening experience on click:
  - Outer cards maintain a 1:1 aspect ratio square container with 3D perspective (`perspective: 1500px`) and scale slightly (`1.05`) when open.
  - The book cover (front side) features a deep navy to aqua blue gradient with golden outlines (`rgba(212, 175, 55, 0.25)`) and wiggling floating icons on hover.
  - Added a hover light sweep reflection effect across the hardcover.
  - When clicked, the cover rotates 145 degrees to the left (`rotateY: -145deg`, `transformOrigin: left center`), revealing the inside page.
  - The inside page has a warm cream paper dot texture (`#faf8f5`), elegant Georgia serif fonts for description text, decorative wave drawings, and a brand-colored glowing border.
  - The icon scales up gently when opened, and the cards can be collapsed by clicking the card again or the "Cerrar Diario" button.
- Refactored the Captain's Logbook click animation into a double-sided page-turn cover:
  - Inside the rotating cover wrapper (`rotateY: -180deg`), created a double-sided card structure using Front Face and Back Face, with hidden backfaces (`backfaceVisibility: 'hidden'`).
  - When closed (`rotateY: 0deg`), the Front Face is displayed (hardcover styling).
  - When opened (`rotateY: -180deg`), the cover swings 180 degrees to the left, and the Back Face is shown as the Left Page of the open book, featuring a paper texture, book title ("Diario de a bordo"), and custom nautical quotes.
  - Styled the Left Page with rounded left corners (`borderRadius: '24px 0 0 24px'`) and a right spine shadow/border.
  - Styled the Right Page (the Inside Page revealed behind it) with rounded right corners (`borderRadius: '0 24px 24px 0'`) and a left spine border.
  - This simulates a realistic open book displaying a left page and right page side-by-side, complete with layout transitions for titles, icons, and description text.
  - Added a glowing blue border outline layout using a custom Framer Motion `layoutId` that dynamically targets the active card.
