# 2026-06-17 Session Notes

## Objective
Refactor the "Nuestra promesa" / "¿Por qué navegar con nosotros?" section (Section4Why.tsx) on the home page so that the cards flip in 3D (revealing detailed explanation on hover/mobile click).

## Actions Taken
1. Added detailed translations (`backTitle`, `backSubtitle`, `backBody`) to all language files:
   - `es.json` (Spanish)
   - `en.json` (English)
   - `eu.json` (Basque)
   - `fr.json` (French)
2. Refactored `Section4Why.tsx` to include `useState` and structured the card contents into a sub-component `PillarCard` that supports:
   - 3D perspective flip container.
   - Separate front and back faces with `backfaceVisibility: 'hidden'`.
   - Desktop hover and mobile tap interaction.
   - Clean color styling: front page has foam background, back page has sleek high-contrast ocean-deep background with bright highlights.
3. Initiated build validation to ensure no TS compile errors.
