# 2026-07-09

- **Task**: Replace `tierra.png` parallax background with a version without background and optimize performance.
- **Actions**:
  - **First iteration**:
    - Found new `tierra.png` (6.06MB) at `C:\Users\User\Desktop\agenc-ia\apps\getxobelaeskola-web\public\images\home\parallax\tierra.png`.
    - Converted to `tierra.webp` (214KB) at full 2752x1536 resolution.
    - Updated query parameters to `?v=4`.
  - **Second iteration**:
    - User updated `tierra.png` again (5.92MB) and requested a lighter size for faster loading.
    - Noticed that other parallax layers (e.g. `velero.webp`) use a width of 1920px.
    - Scaled image dimensions to 1920x1072 (aspect ratio preserved) and converted to `tierra.webp` using quality 75:
      `ffmpeg -i tierra.png -vf "scale=1920:-1" -q:v 75 tierra.webp`.
    - This reduced the final size to **132 KB** (extremely close to the original 125 KB and very lightweight).
    - Updated cache-buster version parameters from `?v=4` to `?v=5` in [layout.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/app/%5Blocale%5D/layout.tsx) and [Section1Hero.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section1Hero.tsx).
- **Security & Privacy Query**:
  - User asked if they can make the repository private now that deployments are handled by Vercel and images by Cloudflare (no longer using GitHub Actions).
  - Confirmed that yes, Vercel connects perfectly to private repos, and there's no dependency on public repos anymore.

