# Memory - 2026-07-02

- Optimized PageSpeed Insights TTFB & Performance:
  - **Middleware Database Call Bypass:** Identified that Next.js middleware was intercepting all routes (including the public landing pages `/`, `/es`, `/en`, `/eu`, `/fr`) and calling a blocking Supabase auth check (`supabase.auth.getUser()`) on every request. This caused a server-side roundtrip delay of **1.0 to 1.5 seconds**, inflating the Time to First Byte (TTFB). Refactored [middleware.ts](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/middleware.ts) to bypass these database auth checks for static public landing pages, allowing Vercel to respond instantly and slash LCP delays.
  - **Mobile LCP Preloading:** Added layout-level preloads for `cloud_only_mini.webp` and `cloud_sun_mini.webp` in [layout.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/app/[locale]/layout.tsx) and set `fetchPriority="high"` inside [Navbar.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/layout/Navbar.tsx) to ensure header clouds (which the browser identifies as mobile LCP targets due to responsive sizing) load at 0ms. Added native `style={{ opacity: 0.95 }}` inline override to the cloud element to ensure it paints immediately on FCP rather than waiting for client-side Framer Motion hydration.
  - **Vercel Analytics & Speed Insights:** Installed `@vercel/analytics` and `@vercel/speed-insights` and integrated them into [layout.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/app/[locale]/layout.tsx) to track production load performance and real-user metrics.
  - **isMobile Viewport Detector Fix:** Restored window resize listener in [Section1Hero.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section1Hero.tsx) to toggle `isMobile` at `1280px` breakpoint, returning the 5 interactive cards to their desktop C-shape layout on the left instead of rendering the mobile bottom row layout on desktop viewports.
  - **Staggered Auto-Play Card Videos:** Refactored [BlobCard.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/blobs/BlobCard.tsx) to preload card videos dynamically in a staggered queue (index 0 at 1000ms, index 1 at 2200ms, etc.) on page load. Once loaded, the card fades out its static thumbnail (miniature) and plays the looping video automatically without requiring hover.
- **Fixed Client-Side Exception (React Error #300):** Fixed a crash occurring when navigating to course pages. The [Navbar.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/layout/Navbar.tsx) component had a conditional early return statement (`if (isAcademy || isAuth) { return null; }`) that was positioned *before* other React hook calls (`useState` and `useEffect` blocks). When navigating between pages where this condition changed, the number of rendered hooks mismatched, throwing React Error #300. Moved the conditional check below all hook declarations.
- **Parallax Layer Alignment Adjustments:** Adjusted bounds of background assets in [Section1Hero.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/components/sections/Section1Hero.tsx) to eliminate pixel gaps exposed during continuous camera sways. Extended the land and sea image (`tierra.webp`) by 5px on the right (adjusting `right` to `-15px` and width to `calc(100% + 25px)`). Extended both the left and right sides of the sky image (`cielo extendido v2.webp`) and clouds video by 12px (updating `left` and `right` to `-112px`/`-117px` respectively, and adjusting widths to `calc(100% + 224px)`/`calc(100% + 234px)`) to keep them fully covered during movement.
- **Payment Success Page Light Theme Style:** Restyled [page.tsx](file:///C:/Users/User/Desktop/agenc-ia/apps/getxobelaeskola-web/src/app/[locale]/student/payment-success/page.tsx) to follow the global brand guidelines (white/off-white background, dark sea-foam text) instead of hardcoding a dark blue layout. Updated the transaction ticket card mockup and background gradients accordingly.
