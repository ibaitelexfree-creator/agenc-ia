# Memory: 2026-07-22

## Summary
- Reduced the size of the floating "Become Member" / "Acceso Socias" button (`SailboatAccesoButton`) on the sailboat inside `Section1Hero.tsx` to a very compact and clean scale.
- Reduced the size of the main Hero CTA GlowButton (linking to `#`) from `xxl` to `lg`.
- Adjusted the interactive video cards (blobs) layout in `Section1Hero.tsx` to align them closer to one another and center them on the screen.

## Details
- Modified `apps/getxobelaeskola-web/src/components/sections/Section1Hero.tsx`:
  - First iteration: Reduced padding of `SailboatAccesoButton` to `12px 24px` and font-sizes to `15px` (`12px` for `eu`).
  - Second iteration: Reduced padding of `SailboatAccesoButton` further to `6px 12px`, letter spacing to `0.08em`, font-sizes to `11px` (`9px` for `eu`), and minimized the red glow textShadow (`2px`, `6px`, `12px`).
  - Main CTA button: Changed `size="xxl"` to `size="lg"` for the garnet `GlowButton` that points to `#`.
  - Bottom Blobs/Videos Layout: Changed `maxWidth` from `1450px` to `1280px` on the absolute outer wrapper. Changed the inner motion container styling from `justifyContent: 'space-evenly'` to `justifyContent: 'center'`, and adjusted the horizontal spacing with `gap: 'clamp(14px, 3.2vw, 42px)'`.
- Updated `project_memory/GLOBAL_STATE.md` with task T-023.
