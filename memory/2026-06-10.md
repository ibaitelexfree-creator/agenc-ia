# 2026-06-10 Daily Log

## Changes Made
- Modified the homepage scroll engine parameters so that Slide 3 (Path) and Slide 4 (Why) retain focus for a portion of the scroll, matching the pinning behavior of Slide 2.
- **Update**: Increased the focus retention duration for slides 3 and 4 even more, and increased `TOTAL_SCROLL_HEIGHT_VH` from `500` to `650` in `scroll-map.ts` to make transitions require more scroll.
- Redefined scroll start/end ranges for each step in `scroll-map.ts`:
  - Slide 1 to 2 transition: `0.0` to `0.12`
  - Slide 2 still (focus): `0.12` to `0.40`
  - Slide 2 to 3 transition: `0.40` to `0.78` (including still focus on Slide 3 from `0.40` to `0.68`)
  - Slide 3 to 4 transition: `0.78` to `0.92` (including still focus on Slide 4 from `0.78` to `0.92`)
  - Slide 4 to CTA transition: `0.92` to `1.0`
- Synchronized `useScrollEngine.ts` interpolation keyframes (`scrollPoints`, `xScrollPoints`, `yScrollPoints`) and values (`rawCanvasX`, `rawCanvasY`, `currentSection`, `prowValues`, `compassValues`).
- Synchronized `RoutePath.tsx` path-length drawing range (`[0.28, 0.78]`) with the new scroll engine ranges.
- **Removed Compass Navigation**: Removed the `CompassNav` indicator (the bottom-right square showing the current slide) from `LandingPageClient.tsx` to clear up the interface.
- **Fixed About Page Text Color**: Fixed text visibility issues on the about page `/es/club/conocenos` (linked to `about/page.tsx`) by replacing hardcoded `text-white` and white borders/gradients with `text-sea-foam` (#1A1A1A) and matching light-theme styling to ensure readability on the white background.
- **Fixed Footer Sponsor Logos for Light Theme**: Fixed text visibility issues on the white page footer for the three sponsor logos (Getxo Kirolak, NextGenerationEU, Plan de Recuperación). Modified SVG files (`logo-nextgen.svg` and `logo-plan-recuperacion.svg`) to change text colors from white to dark gray (`#1A1A1A`). Updated the React component `Footer.tsx` (both under `apps/getxobelaeskola-web` and root `src` directories) to style the `GetxoKirolakLogo` with dark colors (`text-sea-foam/40`, `text-sea-foam/20`) and interactive hover transitions so they light up (using `hover:text-accent` and `hover:opacity-100`) when hovered.
- **Fixed Socias Page Text Color for Light Theme**: Updated `/es/club/socias/` page (`page.tsx` under both root `src` and `apps/getxobelaeskola-web` directories) to use `text-sea-foam` (#1A1A1A) as default, replacement of white borders with `border-sea-foam/10`, card backgrounds with `bg-sea-foam/[0.02]`, and sub-texts to `text-sea-foam/60` to ensure optimal contrast and readability.
- **Fixed Regatas / Experiences Page Text Color for Light Theme**: Updated `/es/club/regatas/` (rendered by `/experiences/page.tsx` along with `ExperiencesClient.tsx` and `ExperienceCard.tsx` in both root `src` and `apps/getxobelaeskola-web` directories) to replace white texts with `text-sea-foam` (#1A1A1A), transparent borders with `border-sea-foam/10`, and filter button/input backgrounds to light theme variants. The card image overlay is set to a darker gradient `from-black/60` to ensure the white price tag remains highly readable.
- **Fixed Que es la Vela / Glosario Page Text Color for Light Theme**: Updated `/es/club/que-es-la-vela/` (rendered by `/glosario/page.tsx` in both root `src` and `apps/getxobelaeskola-web` directories) to change layout text, page heading, search bar, filters and terms grid card background/text colors to `text-sea-foam`, `bg-sea-foam/[0.02]` and matching dark-theme border colors to ensure perfect contrast on the light theme background.
- **Fixed Cursos Page Text Color for Light Theme**: Updated `/es/servicios/cursos/` page (rendered by `courses/page.tsx`, `CoursesListClient.tsx`, `CourseCard.tsx` and `CourseFilters.tsx` under both root `src` and `apps/getxobelaeskola-web` directories) to swap all hardcoded white text variables, border classes (`border-white/10` to `border-sea-foam/10`), filter scroll indicators, and background cards with the standard `text-sea-foam` and light-theme translucent settings.
- **Fixed ComingSoonPlaceholder Text and Border Colors for Light Theme**: Updated `ComingSoonPlaceholder.tsx` (in both root `src` and `apps/getxobelaeskola-web` directories) to replace hardcoded white text (`text-white`, `text-white/50`) with `text-sea-foam` (#1A1A1A) and matching light-theme shadow and border colors. This automatically restores visibility on the light-theme background for multiple services pages that use it, including:
  - `/es/servicios/equipos/`
  - `/es/servicios/udalekuak/`
  - `/es/servicios/centros-escolares/`
  - `/es/servicios/team-building/`
  - `/es/servicios/cumpleanos/`
  - `/es/servicios/material/`
- **Fixed Tienda Page Text and Border Colors for Light Theme**: Updated `/es/servicios/tienda/` (rendered by `tienda/page.tsx` in both root `src` and `apps/getxobelaeskola-web` directories) to replace white text with `text-sea-foam`, border lines with `border-sea-foam/10`, and item backgrounds with `bg-sea-foam/[0.02]`. Kept dark image overlays (`bg-black/20`, `bg-black/40` and `bg-black/50`) intact so product details and purchase actions on image hover continue to pop.
